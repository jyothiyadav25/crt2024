#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n,d,pro=1;
    scanf("%d",&n);
    while(n>0)
    {
      d=n%10;
      n=n/10;
        if(d!=0)
      pro=pro*d;
      
    }

    printf("%d",pro);
    return 0;
}
