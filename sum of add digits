#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
 int n,sum=0;
    scanf("%d",&n);
    while(n>0)
    {
        int x=n%10;
        if(x%2!=0)
        {
            sum=sum+x;
        }
        n=n/10;
    }
        printf("%d",sum);
return 0;
    }
    
